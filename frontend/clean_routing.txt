// Clean routing section - paste this to replace lines 151-195 in login_page.dart

          // ROLE BASED ROUTING - Final clean implementation  
          final userRole = authProvider.currentUser!.role;
          
          print('🎯 LOGIN ROUTING: User Role = "$userRole"');
          
          // Get route destination based on user role
          String routeDestination = _getRouteByRole(userRole);
          
          print('🚀 NAVIGATION: Routing to $routeDestination');
          Navigator.pushReplacementNamed(context, routeDestination);